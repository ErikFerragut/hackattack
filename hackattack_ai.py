from hackattack_player import *
import random

class AI(Player):
    def get_moves(self):
        # decide whether to fortify or expand
        if random.random() < 0.3: # fortify
            pass
        else:                     # expand
            pass
        # is there a hackable thing
        return move
